# Keep the Docker build context clean and reproducible.
# Everything here is rebuilt inside the image or must never be baked in.
node_modules
mobile/node_modules
dist
.git
.gitignore
*.log
npm-debug.log*

# Secrets — provided by Render env vars at runtime, never baked into the image
.env
.env.*
mobile/.env
mobile/.env.*
