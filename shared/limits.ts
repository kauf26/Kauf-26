/** New product listings (photo → publish flow) allowed per calendar day on the server. */
export const DAILY_PRODUCT_CREATE_LIMIT = 25;

/** After this many products created today (inclusive), the next response includes a near-limit warning. */
export const DAILY_PRODUCT_CREATE_WARNING_THRESHOLD = 20;
