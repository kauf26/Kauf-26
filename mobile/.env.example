# Mobile build-time env (Expo). Copy to `.env` in this folder.
# Redirect URI setup: ../docs/mobile-oauth-redirect-setup.md
#
# OAuth authorize/token URLs are defined in shared/marketplaceOAuthManifest.ts
# (eBay, Amazon, Etsy, Shopify, etc. — no placeholder example.com URLs).
# Client IDs are served by GET /api/marketplaces/oauth-config from the server .env.

# REQUIRED — backend API base URL (no trailing slash).
# Local dev (simulator):       http://localhost:2626
# Local dev (physical device): http://YOUR_LAN_IP:2626
# Production:                  https://api.yourdomain.com
EXPO_PUBLIC_API_URL=

# REQUIRED for production EAS builds — web app origin for Privacy Policy & Terms links.
# Example: https://yourdomain.com  →  /privacy and /terms in Settings
EXPO_PUBLIC_WEB_BASE_URL=
# Optional overrides (use instead of WEB_BASE_URL + path):
# EXPO_PUBLIC_PRIVACY_URL=https://yourdomain.com/privacy
# EXPO_PUBLIC_TERMS_URL=https://yourdomain.com/terms

# =============================================================================
# On-device token exchange secrets (server .env holds client IDs)
# =============================================================================
# Etsy uses PKCE only — no mobile secret required.
# TODO: Set ETSY_CLIENT_ID in server .env

# TODO: Set EBAY_CLIENT_ID + EBAY_CLIENT_SECRET in server .env
# Required for on-device eBay token exchange (Basic auth):
EXPO_PUBLIC_EBAY_CLIENT_SECRET=

# TODO: Set SHOPIFY_CLIENT_ID + SHOPIFY_CLIENT_SECRET in server .env
# Required for on-device Shopify token exchange:
EXPO_PUBLIC_SHOPIFY_CLIENT_SECRET=

# TODO: Set AMAZON_CLIENT_ID + AMAZON_CLIENT_SECRET in server .env (server OAuth flow)

# Optional — other OAuth marketplaces that use form_secret / form_basic on device:
# TODO: Set corresponding *_CLIENT_ID in server .env before connecting
# EXPO_PUBLIC_ALIEXPRESS_APP_SECRET=
# EXPO_PUBLIC_ALLEGRO_CLIENT_SECRET=
# EXPO_PUBLIC_AMAZON_CLIENT_SECRET=
# EXPO_PUBLIC_BIGCOMMERCE_CLIENT_SECRET=
# EXPO_PUBLIC_BOLCOM_CLIENT_SECRET=
# EXPO_PUBLIC_FLIPKART_APP_SECRET=
# EXPO_PUBLIC_LAZADA_APP_SECRET=
# EXPO_PUBLIC_MERCADOLIBRE_CLIENT_SECRET=
# EXPO_PUBLIC_RAKUTEN_CLIENT_SECRET=
# EXPO_PUBLIC_SHOPEE_PARTNER_KEY=
# EXPO_PUBLIC_TAOBAO_APP_SECRET=
# EXPO_PUBLIC_WAYFAIR_CLIENT_SECRET=
# EXPO_PUBLIC_WOOCOMMERCE_CLIENT_SECRET=
# EXPO_PUBLIC_ZALANDO_CLIENT_SECRET=
